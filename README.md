# Ripe Apple

A React + Vite app (Tailwind for styling, lucide-react for icons).

## Run locally

```bash
npm install
npm run dev
```

## Build for production

```bash
npm run build
```

Outputs to `dist/`.
